export enum tagTypes {
      auth="auth",
      users="users",
      issues="issues",
      transection="transection",
      dashboard="dashboard",
      products="products",
      category="category",
  }
  export const tagTypesList = [
    tagTypes.auth,
    tagTypes.users,
    tagTypes.issues,
    tagTypes.transection,
    tagTypes.dashboard,
    tagTypes.products,
    tagTypes.category

  ];